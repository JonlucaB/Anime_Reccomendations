export default function displayAnime() {
    /*
     * we need to take in all 20 anime reccomendations from the user input
     * Query AniList for each of them for their ID, or maybe we can do it in one query idk we haven't gotten there yet'
     * Take the data from each anime, send the data over to createAnimeCard, and then take that new element and append it
     * to the grid we will be displaying in the main web page
     * /
}